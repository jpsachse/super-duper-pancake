
function foo(bar: number) {
    // Return the sum of bar and 42
    return bar + 42;
}

class Foo {

    // This is just a random comment without association.

    public bar() {
        // This function simply does nothing.
    }
}
