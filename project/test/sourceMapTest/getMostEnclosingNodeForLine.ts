
foo.bar().baz();

class Foo {

}

anObject.aFunction(withA,
                   veryLong,
                   parameterList);

const bar = () => {
    const aNumber = 5;
    aNumber.toExponential().toLocaleLowerCase().trim();
};
