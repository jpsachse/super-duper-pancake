
// A function call with a comment above
this.isCalling(aFunction); // and a comment behind
// And a comment below

// A comment above a special node
while (true) {
    // A comment below a special node
    some.statement.andACall();
}

// A comment above a special node
for (let i = 0; i < 5; i++) {
    // A comment below a special node
    some.statement.andACall();
}

// A comment above a special node
do {
    // A comment below a special node
    some.statement.andACall();
} while (true)

// A comment above a special node
if (true) {
    // A comment below a special node
    some.statement.andACall();
    // A comment above a special node
} else {
    // A comment below a special node
}

// A comment above a special node
while (true) {
    some.statement.inBetween();
    // A comment below a special node
    some.statement.andACall();
}
